import 'package:stacked/stacked.dart';

class SupportViewModel extends BaseViewModel {
  // Add logic if necessary, e.g., to handle button taps
  
  void startChat() {
    // Action to start chatting with customer service
  }

  void openGuides() {
    // Action to open user guide videos
  }
}
